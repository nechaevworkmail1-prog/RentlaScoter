# 🚀 Инструкция по переносу проекта

## Быстрый старт

### Шаг 1: Подготовка файлов

**Что копировать:**
-  Все файлы из папки проекта
-  Файл `data.json` (если нужно сохранить данные)

**Что НЕ копировать:**
-  Папка `node_modules/` (будет установлена заново)
-  Файлы `.log`

### Шаг 2: Установка на новом устройстве

1. **Проверьте наличие Node.js:**
   ```bash
   node --version
   npm --version
   ```
   Если Node.js не установлен, скачайте с [nodejs.org](https://nodejs.org/)

2. **Перейдите в папку проекта:**
   ```bash
   cd accountant-bot
   ```

3. **Установите зависимости:**
   ```bash
   npm install
   ```
   Это займет 1-2 минуты

4. **Запустите проект:**
   ```bash
   npm start
   ```

5. **Откройте в браузере:**
   ```
   http://localhost:3000
   ```

## 📦 Структура для переноса

```
accountant-bot/
├── server.js              Обязательно
├── package.json            Обязательно
├── package-lock.json       Обязательно
├── .gitignore             Рекомендуется
├── README.md              Рекомендуется
├── data.json             Опционально (создастся автоматически)
│
└── public/                Обязательно
    ├── *.html
    ├── *.js
    └── *.css
```

## ⚙️ Настройка после переноса

### Изменение порта (если нужно)

Откройте `server.js` и измените:
```javascript
const PORT = 3000; // Ваш порт
```

### Восстановление данных

Если у вас есть файл `data.json` с данными:
1. Скопируйте его в корень проекта
2. Убедитесь, что права доступа позволяют запись
3. Запустите сервер

## 🔐 Учетные данные по умолчанию

После первого запуска создается администратор:
- **Логин:** `admin`
- **Пароль:** `admin`

**⚠️ ВАЖНО:** Смените пароль администратора после первого входа!

## ✅ Проверка работоспособности

1. Сервер запустился без ошибок
2. Открывается страница входа
3. Можно войти как администратор
4. Можно зарегистрировать нового пользователя

## 🐛 Частые проблемы

### "Port 3000 is already in use"
- Измените порт в `server.js`
- Или остановите процесс на порту 3000

### "Cannot find module"
- Выполните `npm install` еще раз
- Убедитесь, что `package.json` на месте

### "Permission denied" (Linux/Mac)
- Дайте права на выполнение: `chmod +x server.js`
- Проверьте права на папку проекта

##  Чек-лист переноса

- [ ] Node.js установлен
- [ ] Все файлы скопированы
- [ ] Выполнен `npm install`
- [ ] Сервер запускается без ошибок
- [ ] Открывается страница входа
- [ ] Можно войти как администратор
- [ ] Данные сохраняются (если переносились)

---

**Готово!** Проект готов к работе на новом устройстве.

