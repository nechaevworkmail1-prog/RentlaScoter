# Апетушатор 🐓 - Система управления арендой самокатов

Веб-приложение для управления арендой самокатов с разделением ролей пользователей (администратор и обычный пользователь).

## 📋 Описание проекта

**Апетушатор** - это полнофункциональная система аренды самокатов, которая позволяет:
- Администраторам управлять самокатами и пользователями
- Обычным пользователям арендовать самокаты и просматривать историю поездок
- Отслеживать статистику использования и расходы

## 🚀 Требования

- **Node.js** версии 14.0.0 или выше
- **npm** (обычно устанавливается вместе с Node.js)
- Современный веб-браузер (Chrome, Firefox, Edge, Safari)

## 📦 Установка

### 1. Клонирование/Копирование проекта

Если проект находится в репозитории:
```bash
git clone <repository-url>
cd accountant-bot
```

Если проект копируется вручную, убедитесь, что скопированы все файлы из папки проекта.

### 2. Установка зависимостей

```bash
npm install
```

Эта команда установит все необходимые пакеты:
- `express` - веб-сервер
- `cors` - обработка CORS запросов
- `bcryptjs` - хеширование паролей
- `jsonwebtoken` - JWT токены для аутентификации
- `body-parser` - парсинг тела запросов
- `nodemon` (dev) - автоматическая перезагрузка при разработке

### 3. Первоначальная настройка

При первом запуске автоматически создастся файл `data.json` с начальными данными:
- Пользователь-администратор (логин: `admin`, пароль: `admin`)
- Пустой список самокатов
- Пустой список поездок

**⚠️ ВАЖНО:** Файл `data.json` содержит все данные приложения. При переносе проекта этот файл нужно скопировать отдельно, если вы хотите сохранить существующие данные.

## 🏃 Запуск проекта

### Обычный запуск

```bash
npm start
```

Сервер запустится на **http://localhost:3000**

### Запуск в режиме разработки (с автоперезагрузкой)

```bash
npm run dev
```

Требует установленного `nodemon`. Если его нет, установите глобально:
```bash
npm install -g nodemon
```

### Остановка сервера

В терминале нажмите `Ctrl+C` или закройте окно терминала.

На Windows можно также использовать:
```bash
taskkill /F /IM node.exe
```

## 👤 Учетные данные по умолчанию

### Администратор
- **Логин:** `admin`
- **Пароль:** `admin`

### Обычные пользователи
Создаются через страницу регистрации (`/register.html`)

## 📱 Использование

### Для администратора

1. Откройте браузер и перейдите на **http://localhost:3000**
2. Войдите с учетными данными администратора
3. Вы будете перенаправлены на админ-панель
4. В админ-панели доступны разделы:
   - **Самокаты** - управление самокатами (добавление, редактирование, удаление)
   - **Пользователи** - просмотр и удаление пользователей
   - **Профиль** - информация о профиле и статистика

### Для обычного пользователя

1. Зарегистрируйтесь на странице **http://localhost:3000/register.html**
2. Войдите в систему
3. Вы будете перенаправлены на страницу пользователя
4. Доступные разделы:
   - **Доступные самокаты** - просмотр и аренда доступных самокатов
   - **История поездок** - просмотр всех поездок и завершение активных
   - **Профиль** - информация о профиле и статистика поездок

## 📁 Структура проекта

```
accountant-bot/
├── server.js              # Основной файл сервера (Express)
├── package.json           # Зависимости и скрипты проекта
├── package-lock.json      # Точные версии зависимостей
├── data.json             # База данных (создается автоматически)
├── .gitignore            # Игнорируемые файлы для Git
├── README.md             # Документация проекта
│
└── public/               # Статические файлы (фронтенд)
    ├── index.html        # Страница входа
    ├── register.html     # Страница регистрации
    ├── admin.html        # Админ-панель
    ├── user.html         # Страница пользователя
    ├── styles.css        # Общие стили
    ├── auth.js           # Логика аутентификации
    ├── admin.js          # Логика админ-панели
    ├── user.js           # Логика страницы пользователя
    └── register.js       # Логика регистрации
```

## 🔌 API Endpoints

### Аутентификация

- **POST** `/api/login` - Вход в систему
  - Body: `{ username, password }`
  - Returns: `{ token, user }`

- **POST** `/api/register` - Регистрация нового пользователя
  - Body: `{ username, password }`
  - Returns: `{ id, username, role }`

- **GET** `/api/me` - Получение информации о текущем пользователе
  - Headers: `Authorization: Bearer <token>`
  - Returns: `{ id, username, role }`

### Самокаты

- **GET** `/api/scooters` - Получение списка всех самокатов
  - Headers: `Authorization: Bearer <token>`
  - Returns: `Array<Scooter>`

- **POST** `/api/scooters` - Добавление самоката (только админ)
  - Headers: `Authorization: Bearer <token>`
  - Body: `{ brand, model, serialNumber, status, location, batteryLevel, pricePerHour, pricePerMinute }`
  - Returns: `Scooter`

- **PUT** `/api/scooters/:id` - Обновление самоката (только админ)
  - Headers: `Authorization: Bearer <token>`
  - Body: `{ brand, model, serialNumber, status, location, batteryLevel, pricePerHour, pricePerMinute }`
  - Returns: `Scooter`

- **DELETE** `/api/scooters/:id` - Удаление самоката (только админ)
  - Headers: `Authorization: Bearer <token>`
  - Returns: `{ message }`

### Поездки

- **POST** `/api/rides/start` - Начало аренды самоката
  - Headers: `Authorization: Bearer <token>`
  - Body: `{ scooterId }`
  - Returns: `Ride`

- **POST** `/api/rides/end/:id` - Завершение аренды самоката
  - Headers: `Authorization: Bearer <token>`
  - Returns: `Ride` (с расчетом стоимости)

- **GET** `/api/rides` - Получение истории поездок пользователя
  - Headers: `Authorization: Bearer <token>`
  - Returns: `Array<Ride>`

### Пользователи (только админ)

- **GET** `/api/users` - Получение списка всех пользователей
  - Headers: `Authorization: Bearer <token>`
  - Returns: `Array<User>`

- **DELETE** `/api/users/:id` - Удаление пользователя
  - Headers: `Authorization: Bearer <token>`
  - Returns: `{ message }`

## 💾 Хранение данных

Все данные хранятся в файле `data.json` в корне проекта. Структура данных:

```json
{
  "users": [
    {
      "id": 1,
      "username": "admin",
      "password": "<hashed>",
      "role": "admin"
    }
  ],
  "scooters": [
    {
      "id": 1,
      "brand": "Brand",
      "model": "Model",
      "serialNumber": "12345",
      "status": "available",
      "location": "Location",
      "batteryLevel": 100,
      "pricePerHour": 100,
      "pricePerMinute": 1.67,
      "createdAt": "2025-12-12T..."
    }
  ],
  "rides": [
    {
      "id": 1,
      "userId": 1,
      "scooterId": 1,
      "startTime": "2025-12-12T...",
      "endTime": "2025-12-12T...",
      "status": "completed",
      "durationMinutes": 30,
      "totalCost": 50.1,
      "pricePerMinute": 1.67
    }
  ]
}
```

## 🔒 Безопасность

- Пароли хешируются с помощью `bcryptjs`
- Аутентификация через JWT токены
- Токены действительны 24 часа
- Административные функции защищены проверкой роли

## 🎨 Особенности

- **Логотип и брендинг:** Петух 🐓 и название "Апетушатор"
- **Расчет цен:** Автоматический расчет цены за минуту (цена за час / 60)
- **История поездок:** Полная история с расчетом стоимости
- **Профиль пользователя:** Статистика поездок и расходов
- **Адаптивный дизайн:** Работает на разных размерах экранов

## 🔧 Настройка порта

По умолчанию сервер запускается на порту **3000**. Чтобы изменить порт, отредактируйте переменную `PORT` в файле `server.js`:

```javascript
const PORT = 3000; // Измените на нужный порт
```

## 📝 Примечания для переноса проекта

### Что нужно скопировать:

1. **Все файлы проекта** (кроме `node_modules/`)
2. **Файл `data.json`** (если нужно сохранить данные)
3. **Файл `package.json`** и `package-lock.json`

### Что НЕ нужно копировать:

- Папка `node_modules/` (устанавливается через `npm install`)
- Файлы `.log` (если есть)

### Шаги для переноса:

1. Скопируйте все файлы проекта на новое устройство
2. Убедитесь, что установлен Node.js
3. Откройте терминал в папке проекта
4. Выполните `npm install`
5. Если нужно сохранить данные, скопируйте `data.json` (или он создастся автоматически)
6. Запустите проект командой `npm start`

## 🐛 Решение проблем

### Порт уже занят

Если порт 3000 занят, измените `PORT` в `server.js` или остановите процесс, использующий порт:
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:3000 | xargs kill
```

### Ошибка при установке зависимостей

Убедитесь, что у вас установлена актуальная версия Node.js:
```bash
node --version
npm --version
```

### Данные не сохраняются

Проверьте права доступа к файлу `data.json`. Убедитесь, что приложение может записывать в эту папку.

## 📄 Лицензия

ISC

## 👨‍💻 Разработка

Для разработки рекомендуется использовать:
- `npm run dev` - автоматическая перезагрузка при изменениях
- Открыть DevTools в браузере для отладки
- Проверять консоль сервера для логов

## 📞 Поддержка

При возникновении проблем проверьте:
1. Версию Node.js (должна быть >= 14.0.0)
2. Установлены ли все зависимости (`npm install`)
3. Не занят ли порт 3000
4. Правильность учетных данных

---

**Апетушатор** 🐓 - Ваш надежный партнер в аренде самокатов!
